# ── Web framework ──────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1    # [standard] pulls in websockets + httptools

# ── HTTP client (HuggingFace calls + inter-service calls) ──────
httpx==0.27.0

# ── Data validation ────────────────────────────────────────────
pydantic==2.7.4

# ── Cache (exact + semantic + session store) ───────────────────
redis[asyncio]==5.0.4
numpy==1.26.4                # cosine similarity in semantic cache

# ── LangGraph pipeline ─────────────────────────────────────────
langgraph==0.1.5
langchain-core==0.2.5        # pinned — langgraph pulls this transitively

# ── HuggingFace cold-start retry logic ─────────────────────────
tenacity==8.3.0

# ── Metadata store (query logging) ────────────────────────────
asyncpg==0.29.0

# ── Observability ──────────────────────────────────────────────
prometheus-client==0.20.0

# ── Environment ────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Required for TypedDict + Annotated compatibility in Python 3.11
typing-extensions==4.11.0
aiofiles==23.2.1          # required by FastAPI StaticFiles
